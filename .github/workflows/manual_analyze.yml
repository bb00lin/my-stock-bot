name: ManualStockDiagnostic

on:
  workflow_dispatch:
    inputs:
      stock_list:
        description: '請輸入股票代碼 (多筆請用換行或空格區隔)'
        required: true
        default: '2330 2317'

# 授予寫入權限以便提交檔案回 Repo
permissions:
  contents: write

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    steps:
      - name: 1. 檢出代碼
        uses: actions/checkout@v3

      - name: 2. 設定 Python 執行環境
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 3. 安裝必要套件
        run: |
          pip install yfinance pandas requests FinMind ta tqdm

      - name: 4. 執行深度診斷
        env:
          LINE_ACCESS_TOKEN: ${{ secrets.LINE_ACCESS_TOKEN }}
          LINE_USER_ID: ${{ secrets.LINE_USER_ID }}
        run: python ManualStock.py "${{ github.event.inputs.stock_list }}"

      - name: 5. 歷史回溯 - 自動 Commit 報告回 Repo
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          # 抓取所有手動產出的報告
          git add manual_report_*.txt latest_manual_report.txt
          if git diff --staged --quiet; then
            echo "無新報告產生，跳過提交。"
          else
            git commit -m "📝 手動診斷存檔: ${{ github.event.inputs.stock_list }}"
            git push
          fi

      - name: 6. 雲端存檔備份 - Upload Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Manual-Diagnostic-Report-${{ github.run_id }}
          path: manual_report_*.txt
          retention-days: 7
