name: DailyStockBot

on:
  schedule:
    - cron: '45 6 * * 1-5'
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      
    steps:
      - name: 1. 複製儲存庫代碼
        uses: actions/checkout@v3

      - name: 2. 設定 Python 環境 (3.10)
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 3. 安裝必要套件
        run: |
          python -m pip install --upgrade pip
          pip install yfinance pandas requests FinMind ta tqdm

      - name: 4. 執行股票分析並傳送 LINE
        env:
          LINE_ACCESS_TOKEN: ${{ secrets.LINE_ACCESS_TOKEN }}
          LINE_USER_ID: ${{ secrets.LINE_USER_ID }}
        run: python DailyStockBot.py

      - name: 5. 將所有日期報告 Commit 並 Push
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          # 使用萬用字元抓取所有 report_ 開頭的 txt
          git add report_*.txt latest_report.txt
          if git diff --staged --quiet; then
            echo "今日報告已存在且無變動，跳過提交。"
          else
            git commit -m "📝 存檔每日診斷報告: $(date +'%Y-%m-%d')"
            git push
          fi

      - name: 6. 同步備份至 Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Stock-Reports-All
          # 上傳所有 report 檔案
          path: report_*.txt
          retention-days: 7
