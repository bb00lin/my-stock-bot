name: DailyStockBot

on:
  schedule:
    - cron: '30 08 * * 1-5' # 台灣時間 16:30 執行
  workflow_dispatch:

permissions:
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 50 # 確保 1000 檔掃描有足夠時間執行
    steps:
      - name: 1. 檢出代碼
        uses: actions/checkout@v3

      - name: 2. 設定 Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 3. 安裝必要套件 (包含 Google Sheets 支援)
        run: |
          pip install yfinance pandas requests FinMind tqdm gspread oauth2client

      - name: 4. 建立 Google 金鑰檔案
        run: echo '${{ secrets.GOOGLE_SHEETS_JSON }}' > google_key.json

      - name: 5. 執行股票分析
        env:
          LINE_ACCESS_TOKEN: ${{ secrets.LINE_ACCESS_TOKEN }}
          LINE_USER_ID: ${{ secrets.LINE_USER_ID }}
        run: python DailyStockBot.py

      - name: 6. 自動 Commit 報告
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          # 移除 google_key.json 以免被上傳到公開倉庫（安全性考量）
          rm -f google_key.json
          git add scan_report_*.txt latest_scan.txt || true
          if git diff --staged --quiet; then
            echo "今日無變動"
          else
            git commit -m "📝 每日掃描存檔: $(date +'%Y-%m-%d')"
            git push
          fi
