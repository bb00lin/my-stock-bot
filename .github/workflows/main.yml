name: DailyStockBot

on:
  schedule:
    - cron: '45 6 * * 1-5' # 台北 14:45
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: 1. 複製儲存庫代碼
        uses: actions/checkout@v3

      - name: 2. 設定 Python 環境
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 3. 安裝必要套件
        run: |
          pip install yfinance pandas requests FinMind ta

      - name: 4. 執行股票分析並傳送 LINE
        env:
          LINE_ACCESS_TOKEN: ${{ secrets.LINE_ACCESS_TOKEN }}
          LINE_USER_ID: ${{ secrets.LINE_USER_ID }}
        run: python DailyStockBot.py

      - name: 5. 自動 Commit 報告回 Repo
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add scan_report_*.txt latest_scan_report.txt
          if git diff --staged --quiet; then
            echo "無變動，跳過提交。"
          else
            git commit -m "📝 每日掃描存檔: $(date +'%Y-%m-%d')"
            git push origin main
          fi
