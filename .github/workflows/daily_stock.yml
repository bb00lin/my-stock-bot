name: DailyStockPush

on:
  schedule:
    - cron: '30 6 * * 1-5' # 台北 14:30
    - cron: '10 8 * * 1-5' # 台北 16:10
  workflow_dispatch: 

# 賦予寫入權限以便自動 Commit 報告
permissions:
  contents: write

jobs:
  run_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install System Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install tqdm requests pandas yfinance FinMind ta scikit-learn

      - name: Run DailyStockPush
        env:
          LINE_ACCESS_TOKEN: ${{ secrets.LINE_ACCESS_TOKEN }}
          LINE_USER_ID: ${{ secrets.LINE_USER_ID }}
        run: python DailyStockPush.py

      - name: 歷史回溯 - 自動 Commit 報告回 Repo
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          # 抓取所有每日報告
          git add report_*.txt latest_report.txt
          if git diff --staged --quiet; then
            echo "今日報告無變動，跳過提交。"
          else
            git commit -m "📝 更新每日診斷報告: $(date +'%Y-%m-%d')"
            git push origin main
          fi

      - name: 雲端存檔備份 - Upload Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Daily-Stock-Report-${{ github.run_id }}
          path: report_*.txt
          retention-days: 7
